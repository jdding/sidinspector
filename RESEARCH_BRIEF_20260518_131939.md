# Research Brief：AUDIT-SID

**生成时间**：2026-05-18 13:19:39 CST  
**Active skill**：`/idea-discovery`  
**范围**：围绕 generative recommendation / generative retrieval 中 `semantic ID / tokenizer / codebook / quantization` 的 public-first 评测与诊断方向。

## 问题定义

近期生成式推荐与生成式检索的主要创新，越来越集中在 `semantic ID / tokenizer / codebook` 层。这个方向如果继续卷“再做一个 SID 生成算法”，竞争会非常拥挤。更合适的问题是：

> 能否建立一个公开可复现的诊断框架，解释 semantic-ID tokenizer 什么时候有效、什么时候失效、为什么失效，以及这些内部性质如何影响推荐效果、解码成本、漂移稳定性和可部署性？

## 背景

这轮调查由 2026 Q2 的 paper-watch 信号触发。ReSID、DACT、CapsID、AsymRec、DIG、CQ-SID、AdaSID 等工作都在推动 tokenizer/codebook 成为核心方法对象，而不是实现细节。

本项目已有的优势不是直接做生成式推荐算法，而是评测、诊断和 audit 逻辑。AUDIT-SID 可以看作 AUDIT-T 的表示层对应物：前者审计 temporal evaluation，后者审计 semantic-ID representation / tokenizer。

## 约束

- **数据**：当前阶段 public-first，不使用华为内部数据、业务日志或专有实现细节。
- **实验**：本轮只做 idea-discovery，不启动训练。
- **贡献门槛**：普通 leaderboard 不够，必须产出能改变评价方式的 diagnostic methodology。
- **定位**：方法论 / benchmark / diagnostic paper，不是新 tokenizer algorithm。

## 已知前提

- Public OPE 和 lifecycle-transition evidence-validity 两条线已经因为 novelty 不足关闭。
- 排除 TOIS 后，内部 grounded 的 Top-1 method 方向仍是换机回归推荐。
- 泛化的 SID/tokenizer/codebook 生成算法方向很拥挤，不适合直接进入。
- 用户关心的是评测和诊断这条线是否可以 public-first。

## 非目标

- 不以提出新 SID tokenizer 作为主贡献。
- 不从公开数据推导工业部署结论。
- 不在 public-stage artifact 中包含华为内部数据或业务信息。
- 不在 feasibility gate 之前启动 GPU 实验。

## 本轮成功标准

这轮 pipeline 需要回答：

1. AUDIT-SID 是否有足够 novelty 和 community contribution；
2. 哪些诊断问题最强；
3. 哪些公开数据和开源 baseline 能支撑第一篇；
4. 下一步是否进入 feasibility gate。
