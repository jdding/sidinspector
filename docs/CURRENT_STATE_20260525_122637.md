# AUDIT-SID / SIDInspector Current State

Timestamp: 2026-05-25 12:26:37 CST

## Read First

For current work, read these in order:

1. `docs/CURRENT_STATE.md`
2. `docs/CIKM_SHORT_SUBMISSION_RECORD.md`
2. `docs/WORKSPACE_ORGANIZATION.md`
3. `docs/CIKM_EXPERIMENT_DESIGN.md`
3. `docs/RESOURCE_FRAMING_DECISION.md`
4. `docs/GATE0_DECISION.md`
5. `docs/GATE0A_EVIDENCE_MATRIX.md`
6. `docs/GRID_MUSICAL_SAME_DATASET_CPU.md`
7. `docs/GRID_MUSICAL_3SEED_LOCAL.md`
8. `docs/B2_B3_METHOD_SCREEN.md`
9. `docs/EXPERIMENT_PLAN_CLOSURE_AUDIT.md`
10. `docs/PAPER_FINDINGS_POLISH.md`
11. `docs/ADDITIONAL_EXPERIMENT_PREFLIGHT_CODE.md`
12. `docs/THIRD_METHOD_EVIDENCE_GATE.md`
13. `docs/METHOD_RELEASE_SCOUT.md`
14. `docs/CARD_ORIGINAL_NURQVAE_EVIDENCE_GATE.md`
15. `docs/CONTROLLED_STRESSOR_SELECTION.md`
16. `docs/QUALIFIED_COLLISION_PROBE.md`
17. `docs/CAPACITY_BUDGET_SWEEP.md`
18. `docs/VARIABLE_DEPTH_COST_PROBE.md`
19. `docs/PAPER_CONTROLLER_INTEGRATION.md`
20. `docs/SID_PROBLEM_COVERAGE_AUDIT.md`
21. `docs/AUTHOR_ARTIFACT_EMAIL_DRAFTS.md`
22. `docs/EXPERIMENT_PLAN_FULL_AUDIT.md`
23. `docs/CIKM_RESOURCE_PAPER_PLAN.md`
24. `docs/PAPER_STRUCTURE_REFERENCE_UPDATE.md`
26. `refine-logs/EXPERIMENT_TRACKER.md`

Use `docs/DOCUMENT_INDEX.md` for navigation and `docs/ARCHIVE_INDEX.md` for historical provenance.

## Current Verdict

SIDInspector is the current paper-facing name for the AUDIT-SID project. The
repository and historical artifacts keep the AUDIT-SID internal name for
provenance, but the CIKM draft title is now:

> SIDInspector: A Mapping-First Diagnostic Resource for Semantic-ID Tokenizers

Submission status update: on 2026-05-24 CST, the user reported that the paper
has been submitted to CIKM 2026 as a short paper. See
`docs/CIKM_SHORT_SUBMISSION_RECORD.md`.

PDF refresh update: on 2026-05-25 CST, the active short-paper PDF was refreshed
with three recent arXiv references from the 2026-05-25 paper-watch run:
AKT-Rec (`2605.23310`), VarLenRec (`2605.17779`), and large-scale generative
recommenders (`2605.23312`). This is a reference-coverage refresh only; it does
not add new experiments or broaden the paper's claim boundary.

Prose-polish update: on 2026-05-25 CST, the active short-paper PDF received a
full-text readability pass to reduce repeated `artifact/interface/profile`
phrasing and replace experiment-log style transitions with paper-facing
argumentation. This pass did not change numbers, citations, experiments, or the
resource-interface claim boundary.

The earlier Resource Track planning and audits remain useful provenance for the
diagnostic/interface framing, but current follow-up work should be treated as
post-submission review, artifact hygiene, and response preparation rather than
new v0 evidence generation.

## Active 17-Day Lift Plan

As of 2026-05-20 16:14 CST, the project has reopened a bounded aggressive lift
package for the full paper deadline rather than staying in conservative closure
mode. See `docs/CIKM_17_DAY_8PT_LIFT_PLAN.md`.

The selected path is:

- make the category-prefix D3 inversion the main diagnostic finding;
- matched-capacity GRID defense is now complete on AutoDL port 21551; see
  `docs/MATCHED_CAPACITY_GRID_AUTODL_RESULT.md`;
- D3 vertical replication is locally done for an All_Beauty GRID+controls panel;
  see `docs/VERTICAL_D3_REPLICATION_ALL_BEAUTY.md`;
- B3 bounded D3-vs-ranking context is locally done as a prefix-retrieval proxy;
  see `docs/D3_RANKING_CONTEXT_MUSICAL.md`;
- B6 fixed-reranker validation is locally done; see
  `docs/D3_RANKING_VALIDATION_MUSICAL.md`;
- B7 third vertical now has a real Sports_and_Outdoors GRID/RQ-KMeans
  feature-text export at 20,000 items; see
  `docs/SPORTS_GRID_THIRD_VERTICAL.md`;
- B8 qualified aliasing is available as a backup memorable finding only under
  interaction-qualified early-warning wording; it is not causal collision-harm
  evidence.

- Resource type decision: CIKM v0 is a **diagnostic / interface resource**
  rather than a RecBole/BARS-style coverage resource. The paper should sell the
  adapter contract, validator, D1-D5 diagnostic probes, and controlled mechanism
  probes; GRID/ReSID are worked examples. See
  `docs/RESOURCE_FRAMING_DECISION.md`.
- Gate 0 artifact feasibility: passed.
- Gate 0A: conditional pass for a resource-demo framing.
- Experiment plan closure: closed for the CIKM 2026 Resource v0 evidence
  package. No additional local or GPU experiment is required before the next
  writing/review iteration unless a new evidence gap is explicitly selected.
- Full experiment-plan audit: `docs/EXPERIMENT_PLAN_FULL_AUDIT.md` confirms
  the current v0 plan is closed because required items are complete, negative
  closed, or future-scoped. This is a resource-demo closure, not a full SID
  benchmark closure.
- Internal paper-review status: two local internal review rounds completed;
  current internal score is 8.0/10 for CIKM 2026 Resource Track framing.
- External simulated review target: 8.0/10 under the same CIKM 2026 Resource
  Track target, not a lower weak-accept bar. Round 1 reviewers scored 7.2/7.4
  before packaging fixes and 7.7 after fixes; both indicated 8.0 is reachable
  after commit/tag/push/clean-checkout verification.
- Main contribution: mapping-first SID artifact interface plus D1-D5 diagnostic
  probes, with D6 temporal churn as an optional extension and D7 generation
  traces as a future interface hook. See `docs/DIAGNOSTIC_PROBE_TAXONOMY.md`.
- Main paper evidence: generated vector Fig. 1 artifact pipeline,
  adapter/evidence-role Table 1, same-item-universe Musical worked-example
  Table 2 with sanity/calibration rows, controlled mechanism-probe Table 3, and
  a Section 5 clean-checkout verifier
  paragraph. The current paper has no Table 4.
- Latest paper polish: the draft has been restructured into five Resource Track
  sections: Introduction; Resource Scope and Interface; Diagnostic Design and
  Method Space; Demonstration and Findings; and Availability, Reproducibility,
  and Limits. Round 4 further reframed Gate 3 as resource-scoped diagnostic
  separability, rewrote Fig. 1 as a pipeline plus diagnostic preview, made Table
  1 evidence-only, added GRID three-seed mean/variance and structural-floor
  notation to Table 2, converted Table 3 into a controlled mechanism-probe table,
  and deleted the old artifact-checklist Table 4. The bibliography is expanded
  from 15 to 27 cited entries, and the latest PDF uses the CIKM-specified
  `\documentclass[sigconf,natbib=true,anonymous=true]{acmart}` line, without
  ACM `review` mode or line numbers. The body still reaches page 4; GenAI
  Usage Disclosure appears before References, and References follow naturally
  without a forced page break.
- Latest writing update: B3/B4/Sports evidence is integrated conservatively.
  The abstract/introduction now foreground the category-prefix D3 inversion as
  the diagnostic finding; Section 4 keeps All_Beauty and Sports as auxiliary
  portability/support evidence; Section 5 states that prefix-neighborhood
  retrieval gives D3 context but not downstream Recall/NDCG validation.
- Latest experiment update: B6 and B7 are now real local evidence. B6 uses a
  fixed train-only co-occurrence/popularity reranker over SID-induced candidate
  sets and shows positive D3 association with candidate recall and lightweight
  fixed-reranker Recall/NDCG context on Musical across six artifact/control rows.
  B7 adds a 20,000-item Sports GRID export with complete joins and D1-D5 metrics.
  These strengthen the 8/10 lift package but should be folded into the paper
  without changing the non-leaderboard and non-generator-validation boundary.
- Latest paper integration update: Sections 4 and 5 now include B6/B7 in that
  bounded form. The B6/B7 verifier hardening edit is complete, and artifact
  verifier plus 24 unit tests pass.
- Latest writing-priority update: per user instruction, the active draft now
  prioritizes clear logic over the 4-page limit. Abstract/Introduction/Section 4
  explain how B4 All_Beauty, B6 fixed-reranker, and B7 Sports GRID defend the
  D3 diagnostic finding. The compiled PDF is currently 6 pages; compression is
  intentionally deferred until the argument is stable.
- Citation audit update: `docs/CITATION_AUDIT.md` was refreshed on
  2026-05-20 after a reference recency/accuracy pass. The active paper now has
  31 cited references and 31 BibTeX entries, no missing or uncited keys, and
  corrected metadata for `hua2023indexids`, `singh2023bettersemanticids`,
  `zhu2024cost`, `wang2024letter`, and `ju2026snapchatsid`. DiscRec and
  AsymRec are now cited in the method-space paragraph to close the prior
  recent-coverage gap. The 2026-05-25 reference refresh added `yan2026aktrec`,
  `wang2026varlenrec`, and `xu2026largescalegenrec`.
- Title/abstract update: the paper-facing tool name is `SIDInspector`, and the
  abstract now opens from the missing artifact-inspection interface rather than
  from aggregate-metric limitations. It preserves numeric evidence anchors
  for the 23,742-item Musical example, the 3,749-code GRID feature-text row,
  the 0.9769 aliasing rate, and the 0.9751--0.9769 three-seed stability range.
- Public artifact package now includes `ARTIFACT_QUICKSTART.md`,
  `ARTIFACT_MANIFEST.md`, `requirements.txt`, and
  `tools/verify_paper_artifact.py` for clean-checkout verification.
- Future additional experiments should first pass
  `tools/autodl_audit_sid/preflight_metric_inputs.py`, the local CPU contract
  gate for new SID/metadata/interaction inputs.
- Third named-tokenizer evidence is now gated by
  `docs/THIRD_METHOD_EVIDENCE_GATE.md`: no self-implemented paper-inspired
  method can be counted as named evidence. CARD original `nu-rq-vae` has passed
  local source/import/export-contract preflight only because local compatibility
  repair files fill missing official quantizer modules. It is now failed for v0
  main evidence; QuaSID/AdaSID/CapsID and DIGER remain out of v0 main evidence
  after the official-release scout.
- Author artifact-request emails for DIGER, QuaSID, and CapsID were sent from
  `jdding.cs@gmail.com`; DIGER has replied that datasets and an RQ-VAE
  checkpoint are planned before SIGIR. This remains optional future-adapter
  information, not a CIKM v0 evidence blocker.
- Method-inspired probes are now locally executed and should be framed as
  controlled mechanism probes, separate from named-method coverage:
  `qualified_collision_probe`,
  `capacity_budget_sweep`, and `variable_depth_cost_probe`. The first two are
  strong artifact-table material; the third is D5 structural-cost boundary
  evidence.
- Coverage audit update: `docs/SID_PROBLEM_COVERAGE_AUDIT.md` is the current
  answer to whether D1--D6 cover SID literature concerns. The verdict is
  artifact-level yes for the main failure modes, system-level no: generator
  traces, measured serving latency, downstream ranking impact, and unified
  search-rec behavior remain outside D1--D6 unless D7/system artifacts are
  supplied.
- Not claimed: new tokenizer, SID leaderboard, downstream recommender superiority, faithful TIGER reproduction.

## Evidence Snapshot

| Evidence block | Status | Current role | Main caveat |
|---|---|---|---|
| GRID All_Beauty | done | Cluster A scale/stability evidence, 20k x 3 seeds plus 50k seed42 | cross-dataset, 50k is single seed |
| ReSID Musical | done | Cluster B real named-method evidence | bounded 1-epoch FAMAE -> balanced GAOQ |
| GRID Musical CPU | done | same-item-universe diagnostic contrast against ReSID Musical | processed feature-text input, not raw-text TIGER/GRID reproduction |
| GRID Musical 3-seed | done | same-item-universe stability support for the GRID feature-text row | still controlled feature-text, not faithful raw-text TIGER/GRID |
| Sanity baselines | done | lower-bound controls for metric interpretation | not named methods |
| Method-inspired probes | local done | separate controlled mechanism-probe tables for D1/D2/D4/D5 failure modes | not method coverage; variable-depth result is D5 boundary evidence |
| D3v2 | done | co-occurrence collaborative alignment; Musical plus All_Beauty diagnostic replication | diagnostic proxy, not Recall/NDCG validation |
| DACT D6 | optional | churn/drift demonstration | optional only, not Cluster B replacement |
| All_Beauty B4 vertical D3 replication | done | portability/stressor support for D3 inversion and metric separation | category-prefix uses coarse `category` fallback; not rich hierarchy or downstream validation |
| Musical B3 ranking-context probe | local done | prefix-retrieval context for D3 as a diagnostic signal | low Hit@20 across all rows; not Recall/NDCG validation |
| Sports proxy/control D3v2 supplement | remote supplement done | directional portability support for D3 gap | ReSID and CARD rows are proxy exports; not main named-method evidence |
| MovieLens portability | optional done | non-Amazon schema smoke | sanity SIDs only, not main empirical evidence |
| CARD original `nu-rq-vae` preflight | failed for v0 main evidence | source/import/export-contract smoke only | official tree lacks required quantizer modules; local repair changes core assignment semantics |

## Same-Item-Universe Musical Diagnostic Row

| System | Method | Items | Unique SID | Duplicate SID rate | Full collision rate | D3 L1 weighted recall | D4 head/mid/tail unique ratio |
|---|---|---:|---:|---:|---:|---:|---|
| GRID feature-text | `grid_official_rqkmeans_resid_feature_text` | 23742 | 3749 | 0.842094 | 0.976876 | 0.055176 | 0.353001 / 0.358952 / 0.369494 |
| ReSID GAOQ | `resid_gaoq` | 23742 | 23742 | 0.000000 | 0.000000 | 0.153544 | 1.000000 / 1.000000 / 1.000000 |

This is the main response to the third external audit's cross-dataset critique.

Local stability update: `docs/GRID_MUSICAL_3SEED_LOCAL.md` adds GRID Musical
feature-text seeds 43/44 to the original seed 42. All three seeds have complete
joins; duplicate SID rate is 0.8327--0.8421 and full-collision rate is
0.9751--0.9769. This supports the claim that the high collision pressure is
not a single-seed accident, while preserving the feature-text caveat.

## Current Paper Shape

Main paper should contain:

1. artifact schema and adapter contract;
2. Fig. 1 artifact pipeline and D1-D5 main / D6-D7 extension boundary;
3. D1-D5 diagnostic-probe definitions, explicitly scoped to item-to-SID artifacts;
4. facet-aware method coverage table with caveats;
5. same-item-universe Musical diagnostic table;
6. Section 5 availability and clean-checkout verifier contract;
7. controlled mechanism-probe findings in Section 4, with full probe tables
   kept in the artifact repository;
8. resource usage modes, extension contract, and claim-discipline paragraphs.
9. internal-review trail: `docs/INTERNAL_REVIEW_ROUND1.md` and
   `docs/INTERNAL_REVIEW_ROUND2.md`.

Appendix or optional:

- GRID All_Beauty scale/stability;
- DACT D6 churn;
- MovieLens portability;
- AutoDL staging/provenance.

## Current Compute Policy

Run locally first. AutoDL is currently no-GPU and should only be used for staging or verification until a specific GPU-only blocker is documented.

Do not launch robust/sweep/quality queues by default.

## Remaining Non-Experiment Gaps

1. The 2026-05-20 citation drift check is complete; repeat it only if the
   bibliography or cited related-work prose changes before submission.
2. D3 neighborhood alignment is not yet validated against downstream Recall/NDCG.
3. TIGER is not reproduced; paper needs a short justification for GRID.
4. 50k GRID All_Beauty is single-seed and should be appendix/preliminary unless more seeds are added.
5. Current ACM draft includes generated vector Fig. 1 plus Tables 1--3 and
   compiles to `paper/main.pdf` in 5 pages total: the core body fills through
   page 4; GenAI Usage Disclosure appears before References. The latest
   structure/reference pass is recorded in
   `docs/PAPER_STRUCTURE_REFERENCE_UPDATE.md`.
   The class option is now the CIKM short-paper literal form
   `sigconf,natbib=true,anonymous=true`. Local `acmart` reports these key-value
   options as unused global options, but the log confirms review mode is not
   used and the PDF has no line-number gutter.
6. D2 now includes a bounded interaction-qualified mechanism probe, but it is still
   not strict causal collision harm.
7. Full generator behavior is out of scope unless optional `generator_outputs`
   are available for D7 generation-trace diagnostics.
8. `docs/SID_CLUSTER_DIAGNOSTIC_REFRESH.md` is now the current paper-facing
   taxonomy: Cluster B should be described by B1-B4 facets, not as one
   homogeneous recent-method bucket.
9. `docs/PAPER_STRICT_CLAIM_AUDIT.md` is the current wording/number audit:
   it now checks the mechanism-probe numeric claims and finds no hard overclaim,
   but final pre-submission drift check remains.
10. The prior 8.0/8.1 external-review gate has been superseded by the stricter
   R3 review and the 17-day lift plan. The third-method screen remains negative,
   but the same-item A/B panel is now strengthened by GRID Musical three-seed
   stability and a completed matched-capacity GRID ablation. Fig. 1 has been
   redrawn as an artifact-contract/diagnostic preview. The active open question
   is no longer whether the matched-capacity ablation exists, but whether the
   next external review agrees that it sufficiently lowers the W2 capacity
   confound.
11. The next-method selection entry point is
    `docs/METHOD_DIAGNOSTIC_SELECTION_MATRIX.md`, which maps candidate
    methods/facets to D1-D7, finding material, priority, and screen actions;
    use `docs/DIAGNOSTIC_PROBE_TAXONOMY.md` for current paper-facing names.
12. External simulated review Round 2 closes the requested 8/10 gate:
    the pushed tag `audit-sid-cikm-resource-v0.1` resolves to `d24dfec`,
    a fresh tag clone passes the public verifier and stays clean, and external
    reviewers score the artifact 8.0/10 and 8.1/10 with no P0 blockers.
13. Final submission metadata still needs real single-blind author and
    affiliation information plus a frozen artifact branch or release tag.
14. `docs/B2_B3_METHOD_SCREEN.md` closes the current third-method screen with
    no new main-evidence method: QuaSID/AdaSID/CapsID are paper/motivation only
    in this pass, DIGER is incomplete for artifact export, and CARD remains
    proxy/control unless the original `nu-rq-vae` path is repaired and reviewed.
15. `docs/EXPERIMENT_PLAN_CLOSURE_AUDIT.md` is the current experiment-closure
    entry point: remaining work is citation drift, real single-blind metadata,
    optional copy-editing, and final claim audit after text changes.
16. `docs/THIRD_METHOD_EVIDENCE_GATE.md` is the current admission rule for a
    third named method. A self-implemented paper-inspired method remains a
    controlled mechanism probe, not named evidence.
17. `docs/METHOD_RELEASE_SCOUT.md` records the official-release screen:
    QuaSID/AdaSID/CapsID have no located official artifact, while DIGER's
    official repo is still illustrative/reference and lacks the data/checkpoint
    assets needed for AUDIT-SID v0.
18. `docs/CARD_ORIGINAL_NURQVAE_EVIDENCE_GATE.md` closes the CARD original
    route for v0 main evidence: the official CARD tree is missing the quantizer
    modules required by `nu-rq-vae`, so local compatibility repair cannot be
    attributed as faithful CARD.
19. `docs/DIAGNOSTIC_PROBE_TAXONOMY.md` is the current naming source:
    D1-D5 are main mapping-level probes, D6 is optional temporal churn, and D7
    is a generator-trace hook only. `docs/CONTROLLED_STRESSOR_SELECTION.md`
    remains the historical execution note for the three method-inspired probes.
    Use
    `docs/QUALIFIED_COLLISION_PROBE.md`,
    `docs/CAPACITY_BUDGET_SWEEP.md`, and
    `docs/VARIABLE_DEPTH_COST_PROBE.md` as the result entry points. Keep all
    three outside named-method coverage. `docs/PAPER_CONTROLLER_INTEGRATION.md`
    records the writing pass that folds their compact findings into Section 3.
20. `docs/AUTHOR_ARTIFACT_EMAIL_DRAFTS.md` records the sent DIGER, QuaSID, and
    CapsID artifact-request messages plus the DIGER author reply. These are
    optional future-adapter leads, not current paper evidence.
21. `docs/EXPERIMENT_PLAN_FULL_AUDIT.md` is the latest full closure report.
    It classifies Gate 0, conditional Gate 0A, Gate 1, D1-D5 legacy metric
    evidence, same-item Musical evidence, mechanism-probe evidence,
    verification, and remaining work.
    The verdict is closed for CIKM Resource v0 under conservative
    resource-demo framing. Its Round 4 addendum supersedes older Table 4
    checklist wording.
22. `docs/RESOURCE_FRAMING_DECISION.md` is the current resource-shape decision:
    Type 4 diagnostic/interface resource for CIKM v0; Type 1 coverage resource
    remains a possible post-v0 platform route only after substantially more
    faithful adapters exist.
23. The 2026-05-20 13:36 CST terminology-sync pass closes the immediate
    paper/code naming cleanup. Active paper prose, Fig. 1, reviewer quickstart,
    artifact manifest, and CLI-facing descriptions now use the paper-facing
    `SIDInspector` name plus D1-D5 diagnostic-probe terminology: utilization,
    aliasing, neighborhood alignment, popularity allocation, and structural
    cost. Legacy internal function names and CSV filenames such as
    `d2_collision.csv` and `d5a_deployment_cost.csv` are intentionally
    preserved for artifact provenance and verifier compatibility.
24. Verification after the terminology-sync pass: regenerated Fig. 1, rebuilt
    `paper_assets/tables/table1`--`table6`, passed
    `tools/verify_paper_artifact.py`, passed 16 unit tests, passed
    `git diff --check`, and compiled `paper/main.pdf` successfully. The PDF is
    5 pages total with body through page 4 and References on page 5. No
    undefined citations/references were found. Remaining submission-hygiene
    blockers are real single-blind author metadata, final citation drift check,
    and a final strict claim audit after any further text changes.
25. The current paper-facing title/tool name is `SIDInspector`; AUDIT-SID
    remains the repository/provenance name. The final author block is not yet
    closed: `paper/main.tex` still uses the anonymous local draft block because
    real single-blind author, affiliation, country, and email metadata must be
    supplied before the submission PDF is frozen.
26. R3 simulated CIKM 2026 Resource Track review is now the active
    pre-submission risk register. The review scored the draft 5/10
    borderline/weak-reject leaning, with three high-priority risks:
    thin named-method coverage, GRID-vs-ReSID capacity mismatch, and
    over-strong D3/pre-training-gate wording. Immediate paper-facing fixes are
    in place: Section 2 now gives a compact adapter schema, Section 4 elevates
    the category-prefix D3 inversion as a diagnostic warning rather than a
    quality win, Section 5 softens D3 to a triage signal, and D6/D7 are scoped
    as optional extension / hook evidence. The matched-capacity GRID ablation
    was first stopped locally by CPU stop-loss, then completed on the AutoDL
    GPU instance as the targeted W2 follow-up. It is now current paper evidence
    as the `GRID ft-cap` row, with the caveat that it is a prefix-capacity
    ablation rather than a faithful TIGER/GRID reproduction. Post-edit checks
    pass: `paper/main.pdf` compiles to 5 pages,
    `tools/verify_paper_artifact.py` passes, 16 unit tests pass, `git diff
    --check` passes, and the LaTeX log has no undefined references/citations,
    overfull boxes, or invalid math-mode warnings.
27. `docs/AUTODL_MATCHED_CAPACITY_GRID_PLAN.md` prepares the only recommended
    immediate AutoDL/GPU follow-up: one bounded GRID/RQ-KMeans
    matched-capacity ablation on Musical with per-level widths `32,1280,1280`.
    It is meant to address R3 W2, not to expand the whole experiment matrix.
    Status is now `COMPLETED_AUTODL_GPU` on port 21551. The run exited 0 after
    a successful GPU-utilization check and produced complete D1-D5 metrics:
    9,874 unique full SIDs, D2 full-code aliasing 0.778452, D3 L1
    co-occurrence prefix recall 0.0796, D4 tail unique-SID ratio 0.639064,
    and D5 prefixes `32/9300/9874`. The paper Table 2 now includes this row as
    `GRID ft-cap`, preserving the caveat that it is a prefix-capacity ablation,
    not a faithful TIGER/GRID reproduction.
28. `docs/PAPER_STRICT_CLAIM_AUDIT.md` has been refreshed after integrating
    `GRID ft-cap` into the abstract and Table 2. Verdict is PASS under
    conservative resource-demo framing. The audit fixed the active D3 rounding
    display to `.0796` and confirms that the PDF, artifact verifier, 16 tests,
    diff whitespace check, and LaTeX log scan all pass. Remaining review risks
    are method-coverage width, D3 downstream validation, and final public
    artifact/tag refresh if the matched-capacity result should be reviewer
    reproducible rather than local AutoDL provenance.
29. `docs/VERTICAL_D3_REPLICATION_ALL_BEAUTY.md` records a completed
    All_Beauty vertical-replication panel for the D3 inversion finding. On All_Beauty
    20k, category-prefix reaches D3-L1 0.968 while the GRID feature-text export
    reaches 0.081; on All_Beauty 5k, category-prefix reaches 0.945 while GRID
    reaches 0.075. The category control uses coarse `category` fallback
    metadata, so this supports diagnostic-context portability rather than rich
    hierarchy or downstream Recall/NDCG validation.
30. `review-stage/EXTERNAL_REVIEW_R3_DELTA_RESULT.md` records the targeted
    external R3-delta review after `GRID ft-cap`. The score improved only to
    6/10. W2 is now partially resolved and downgraded from high to moderate,
    but W1 named-method coverage still caps the paper around 6--7 and W3 D3
    downstream grounding remains a P1 concern. Do not describe the current
    draft as 8/10-ready after this review.
31. After the R3-delta result, the paper received a bounded wording patch:
    Section 4 now says `GRID ft-cap` is not a full item-unique leaf match and
    that additional capacity could reduce aliasing further; Section 4 also
    adds the All_Beauty 20k D3 replication sentence; Section 5 states that the
    current demonstration covers two controlled public export paths rather than
    broad tokenizer coverage. The updated PDF still compiles to 5 pages, and
    the verifier, 16 tests, `git diff --check`, and log scan pass.
32. After the latest subagent reviewer-attack pass, the active paper wording
    was tightened again to prevent a leaderboard or faithful-reproduction
    reading. The abstract now says `two controlled public SID export examples`
    and `artifact profiles rather than tokenizer-quality rankings`; the
    method-space table labels GRID/ReSID as controlled/bounded exports; B3 is
    described as prefix-candidate context rather than Recall/NDCG validation;
    and Sports remains proxy/control supplement evidence outside the main
    table. Final checks pass: no flagged stale-name/leaderboard/D3-overclaim
    strings in active paper files, `paper/main.pdf` metadata title is
    `SIDInspector: A Mapping-First Diagnostic Resource for Semantic-ID
    Tokenizers`, and 18 unit tests pass.
33. B3 robustness is now extended from 1,000 to 5,000 Musical users. The
    direction is stable: D3 and prefix-candidate coverage remain broadly
    aligned, while Hit@20 remains low across all rows. This supports D3 as
    ranking-context evidence for the artifact, but it still does not validate
    D3 as a Recall@K/NDCG surrogate.
34. All_Beauty vertical D3 is now three-seed rather than seed42-only. On
    All_Beauty 20k, GRID seed42/43/44 D3 L1 weighted is
    `0.0811/0.0872/0.0898`, while category-prefix remains `0.9684`. This
    strengthens the second-dataset direction for the D3 inversion, but the
    category-prefix row still uses coarse `category` fallback metadata, so the
    paper must keep the stressor/portability boundary.
35. B5 remains a release-watch item, not an implementation item. The 2026-05-20
    recheck did not find a new official item-to-SID mapping, checkpoint package,
    or processed artifact for DIGER, QuaSID, AdaSID, CapsID, or faithful CARD
    that can pass the third-method gate. Do not start a paper-inspired third
    named tokenizer implementation for the current CIKM sprint.
36. The active strategy is now an aggressive 8/10 acceptance attempt, not a
    borderline safety plan. Keep claim boundaries strict, but pursue harder
    evidence: B6 small ranking validation for D3 under a fixed
    mapping-dependent protocol, B7 a third vertical with a real learned/export
    row if feasible, and B8 promotion of qualified aliasing as a backup
    memorable finding. B5 remains no-go as named-method evidence, though a
    clearly labeled SIDInspector reference adapter can still be considered if
    cheap and non-confusing.
37. The 2026-05-20 20:25 writing polish completed a second clarity-first pass
    over the active draft. The abstract now opens from the missing inspection
    interface for exported SID mappings, states that SIDInspector is a
    diagnostic resource rather than a tokenizer or benchmark suite, and keeps
    the central D3 inversion plus B4/B6/B7 support in one narrative. The
    introduction now frames the contribution as an inspectable mapping
    contract plus probes, and Section 4 now presents the Musical worked example
    plus All_Beauty, fixed-reranker, and Sports checks as answers to reviewer
    threats rather than as an experiment log. Section 5 was tightened around
    the resource-interface boundary. Current checks pass: `paper/main.pdf`
    compiles to 6 pages by design, PDF metadata title is `SIDInspector: A
    Mapping-First Diagnostic Resource for Semantic-ID Tokenizers`,
    `tools/verify_paper_artifact.py` passes, 24 unit tests pass, `git diff
    --check` passes, and the LaTeX log scan finds no undefined
    references/citations or overfull boxes. No AutoDL GPU was started for this
    writing-only pass.
38. The sixth-round closure items are now partially closed. A simulated-review
    decision note is frozen at `docs/SIMULATED_REVIEW_V2_DECISION.md`. The W2
    All_Beauty caveat is now explicit in Section 4: the 0.968 category-prefix
    D3 value is treated as dataset-level taxonomy/co-occurrence alignment
    exposed by D3, not as evidence that category tokenizers are universally
    better. Figure 1 was regenerated as a finding preview rather than a generic
    pipeline figure. B6 was replicated on a bounded All_Beauty temporal-LOO
    split: across four rows, D3 has Spearman `1.0000` with candidate recall and
    `0.8000` with fixed-reranker Recall@20/NDCG@20/MRR@20; this is
    supplementary ranking-context evidence only because the native local
    All_Beauty interactions are splitless. `rqvae_minimal_reference` now passes
    a full-Musical local CPU gate with 23,742 items, 17,247 unique full SIDs,
    D2 aliasing `0.4401`, D3 `0.0650`, D4 tail `0.8831`, and D5 prefixes
    `32;2368;17247`; it is admitted only as a reference adapter, not third
    named-method coverage. Current checks pass: `paper/main.pdf` compiles to 6
    pages, `tools/verify_paper_artifact.py` passes, 26 unit tests pass, and
    `git diff --check` passes. No AutoDL/GPU was started.
39. Seventh-round review absorption is now closed as a writing-only patch.
    Section 4 explicitly verbalizes the D2/D3 dissociation: RQ-style aliasing
    drops from GRID ft to GRID ft-cap to RQ-min, while D3 remains in the
    `0.0519--0.0796` band. The All_Beauty B6 paragraph now reports the
    four-row temporal-LOO replication as supplementary directional evidence
    rather than a statistical claim. Figure 1 labels were cleaned up
    (`GRID .05-.08`; `Cross-vertical + alias risk`), and the abstract final
    sentence now says SIDInspector turns tokenizer exports into inspectable
    resource artifacts. Current checks pass: `paper/main.pdf` compiles to 6
    pages, PDF metadata title is `SIDInspector: A Mapping-First Diagnostic
    Resource for Semantic-ID Tokenizers`, `tools/verify_paper_artifact.py`
    passes, 26 unit tests pass, `git diff --check` passes, and the LaTeX log
    scan finds no undefined references/citations or overfull boxes. No new
    experiment or AutoDL/GPU run was started; next work is body-page
    compression.
