# Findings

Append-only cross-stage discovery log for the lifecycle OPE preflight project.

## 2026-05-17

- The initial public-stage framing is `Lifecycle-stratified OPE reliability protocol under sparse support`.
- The first execution gate is public dataset feasibility, not model training.
- Current preferred public dataset order is Open Bandit Dataset / OBP, KuaiRand, KuaiRec, then MIND only as a weaker impression-scale auxiliary.
- Strict OPE claims require logged propensities, randomized exposure, or a clearly bounded estimated-propensity design.
- After expanding to 10 public-data ideas, the recommended novelty-check shortlist is: lifecycle-state OPE credibility protocol, lifecycle-conservative policy selection, and lifecycle-targeted exploration budget simulator.
- Strict novelty check verdict: proceed with lifecycle-state credibility only as a protocol/resource contribution (5/10); treat lifecycle-conservative policy selection as a cautious baseline due to group-constrained LCB overlap (3/10); treat lifecycle-targeted exploration as a cautious simulator/logging-design module (4/10). The main killer prior is DataCOPE plus high-confidence/pessimistic OPE selection; avoid claiming a new estimator.
- After novelty check, `refine-logs/EXPERIMENT_PLAN.md` was downgraded from a full execution plan to a gated schema-feasibility/protocol-preflight plan. The next gate is dataset-field audit and D0/D1/D2 readiness labeling, not model training.
- User rejected a pure resource/protocol target as below personal ambition. Early pre-novelty snapshots and provisional plans were deleted to avoid polluting later work; the live direction is now method-first refinement.
- Research-wiki reassessment changed the main conclusion again: do not continue OPE as the next mainline. Existing dormant-return evidence says gap-duration methods, simple routing, representation adapters, and public OPE are weak next-paper engines. The strongest problem candidate is preference evidence validity / lifecycle transition decision under stale, sparse, and conflicting evidence.
- Fresh idea-discovery on `codex/lifecycle-transition-reassessment` ranks Evidence Validity Modeling for Lifecycle Transitions first. First-pass novelty is 6/10: proceed only if the method defines an explicit validity target and does not collapse into CASP/CALB source gating.
- User decided 6/10 is not worth experiments. Both exploratory branches are closed as no-experiment. Future work is recorded as three separate workstreams: TOIS 5+1 synthesis, internal device-switch return recommendation, and LLM-assisted RecOps.
- REA review narrows LLM-assisted RecOps: first viable version should be a Ranking Experiment Agent for offline/nearline ranking experiment planning, execution, failure handling, and result analysis, not an end-to-end RecOps copilot.
- TOIS is now explicitly excluded from "Top-1 new direction" ranking because it waits for AC/review outcomes. After incorporating the 2026-05-18 semantic-ID/tokenizer/codebook trend, the recommended Top-1 new method direction is device-switch return recommendation with adaptive semantic evidence tokenization; generic SID/tokenizer work is treated as too crowded unless grounded in this switch-back scenario.

## 2026-05-18

- New AUDIT-SID idea-discovery branch created for public-first evaluation/diagnosis of semantic-ID tokenizers/codebooks. Pipeline verdict: proceed with caution, novelty about 7/10, but only as a diagnostic methodology paper. Pure public SID leaderboard is abandoned as low novelty. Next gate is code/artifact feasibility: at least two public tokenizer implementations must export item-to-SID mappings and generator outputs before experiments.
- AUDIT-SID venue plan corrected by paper type: because AUDIT-SID is evaluation/resource/reproducibility work, prioritize venues with explicit Resource/Reproducibility/Evaluation/Datasets tracks. Main target is SIGIR 2027 Resource/Reproducibility-style track if available; RecSys 2027 Resource/Reproducibility and CIKM 2027 Resource are natural backups. WSDM 2027 main is suitable only if the work yields strong empirical findings beyond toolkit/resource value.
- Venue strategy changed again due to recency pressure: target CIKM 2026 Resource Track as the immediate submission because it explicitly accepts dataset/protocol/software/open-source framework/evaluation-tool resources and has the nearest matching deadline. Abstract is due 2026-05-30 AoE and paper is due 2026-06-06 AoE. Gate 0 must pass by 2026-05-24 or the submission should be abandoned.
- CIKM v0 execution scope frozen: primary dataset is ReSID processed Amazon-2023 `Musical_Instruments`, backup is Amazon 2014 Beauty/Sports; must-run methods are RQ-VAE/TIGER-style SID, ReSID, and a sanity ID baseline; must-have diagnostics are codebook utilization, collision harm, semantic-collaborative alignment, and head-tail capacity allocation. DACT/drift and deployment-cost proxy are optional only if cheap.
- User raised a valid risk that RQ-VAE + ReSID plus shallow analysis may not be representative enough to justify community contribution. Method representativeness is now a hard Gate 0A: the selected methods must cover canonical SID plus a meaningful recent tokenizer innovation, and the analysis must reveal a nontrivial diagnostic finding; otherwise abandon CIKM 2026 rather than submitting a weak resource paper.
- Current docs were only partially sufficient for the user's cluster-first proposal, so `docs/SID_METHOD_CLUSTER_AUDIT.md` was added. The CIKM must-run rule is now Cluster A canonical SID + Cluster B/C recent tokenizer innovation + sanity lower bound. Evaluation is defined by questions, not leaderboard metrics: D1 capacity utilization, D2 harmful collision, D3 semantic-collaborative mismatch, and D4 head-tail capacity.
- A subagent-backed Gate 0A framework was added in `docs/METHOD_REPRESENTATIVENESS_AUDIT.md`: methods are scored on representativeness, artifact availability, diagnostic coverage, and sprint cost; CIKM v0 requires at least one Cluster A method and one Cluster B/C method scoring >=8 plus a sanity baseline. Concrete method scores remain pending repo/artifact audit.
- Correction after taxonomy review: the old Cluster B/C split is deprecated because recommendation-native tokenizer design and collision/codebook design do not provide useful decision separation. The live rule is now Cluster A canonical SID + Cluster B recent tokenizer/codebook innovation + sanity lower bound, with Cluster B facets B1 rec-native/predictability, B2 collision/capacity/adaptive codebook, and B3 ranking/retrieval alignment.
- External review absorption changed the CIKM sprint contract from research-finding-first to resource-first: toolkit interface, Method Coverage Table, D1-D5a diagnostics, and a non-redundant case study are the core; strong empirical findings are stretch. Public code screening prioritizes GRID/RQ-VAE for Cluster A, ReSID for Cluster B, CARD as fallback, and treats DIGER as backup only; CapsID/AdaSID/AsymRec remain future support unless runnable code appears. D5a lightweight deployment-cost proxy is now required, while D5b generator-output cost and D6 drift remain optional.
- Gate 0 repo audit found explicit artifact paths for the fast sprint: GRID `rkmeans_inference_flat` writes item-keyed `cluster_ids` via `LocalPickleWriter`, ReSID GAOQ writes `item_feature/item_code_mapping.parquet` with `item_id` and three codebook columns, and CARD `generate_code.py` writes `.npy` codes plus `_item_ids.npy`. Continue to dataset schema probe/export smoke; do not claim full Gate 0 pass until a local SID assignment table is generated.
- ReSID `Musical_Instruments` dataset schema passes Gate 1 for CIKM v0: 23,742 contiguous item IDs, item features `store_id/cate1_id/cate2_id/cate3_id`, train/valid/test interaction parquet files with `user_id/history/target/timestamp`, and zero missing item-feature joins across split item coverage. The adapter smoke generated normalized `item_metadata`, `interactions`, and three sanity SID baselines; the remaining Gate 0 blocker is real ReSID/GRID tokenizer mapping export.
- D1-D5a metric smoke passes on sanity SID baselines: the runner emits utilization, collision, semantic-alignment proxy, head-tail capacity, and SID-trie deployment-cost tables. This validates toolkit plumbing and diagnostic sensitivity only; it does not satisfy Gate 0 until a real ReSID/GAOQ or GRID/RQ-VAE mapping is exported and normalized.
- GRID output-format adapter smoke passes on a synthetic transposed `merged_predictions_tensor.pt`, so the toolkit can normalize GRID-style SID tensors once available. The local GRID clone does not include pretrained embeddings, trained SID checkpoints, or pre-exported mappings, so GRID remains blocked on upstream artifact generation or download.
- Real mapping preflight found no immediate local item-to-SID export: ReSID requires FAMAE `best_model.pth`/`model.pth` before GAOQ writes `item_code_mapping.parquet`, and GRID requires data embeddings plus a trained SID checkpoint before `merged_predictions_tensor.pt` exists. Gate 0 remains open; next step is bounded ReSID export, GRID artifact acquisition/generation, or CARD fallback.
- ReSID run preflight is blocked in the current Python environment because `k_means_constrained` is missing and `model.gaoq` fails at import time. ReSID configs also default to `last_one_out`, CUDA, and 500 FAMAE epochs, while the downloaded dataset is `leave_one_out`; a bounded export requires dependency installation plus explicit config/path adjustment.
- First sub-agent code review found that the initial smoke code could bias popularity/head-tail/collision conclusions. The fix pass changed ReSID interactions to target-only events, made metrics dataset-aware, added item-id coverage validation, made GRID dense-ID assumptions explicit, sorted ReSID codebook levels by numeric suffix, and renamed the intentional collision sanity baseline.
- Local ReSID FAMAE 1 epoch -> GAOQ produced the first real Cluster B mapping: `resid_gaoq` has 23,742 normalized SID rows, zero duplicate full SIDs, coverage pass, D1 utilization across 32/40/19 codes, and D5a prefix counts `32;1280;23742`. Gate 0 is materially advanced but not complete because Cluster A canonical SID coverage is still missing.
- AutoDL staging is prepared for the user's fixed instance shape: 25 CPU cores, 90 GB RAM, 1 x RTX 5090. The current plan is to use AutoDL for ReSID robustness/quality variants and any heavier Cluster A/CARD fallback, while continuing local development and validation first.
- First diagnostic case study is available: ReSID/GAOQ vs three sanity baselines on `Musical_Instruments`. The metric runner now supports mixed SID depths, and the demo shows ReSID has zero full collisions and intermediate category alignment (`0.5669`) while differing from pure category-prefix and collision-heavy controls.
- Second code review fix pass tightened metric semantics: D2 now separates full-collision and prefix-collision popularity, partial coverage fails by default, multi-dataset combined inputs require dataset-aware metadata/interactions, and `sanity_category_prefix` no longer relies on an unsafe pandas `.get()` default.
- Local RQ-KMeans feature-proxy baseline generated a canonical-style stressor with the same nominal 32/40/19 depth-width profile as ReSID but only 2,229 unique SIDs and 90.6% duplicate full-SID rate. This is useful for local toolkit development and case-study contrast, but it does not replace the required public Cluster A implementation export.
- Cluster A preflight now separates GRID and CARD operationally: GRID remains the most representative canonical SID implementation but is dependency/data heavy; CARD is prepared as an AutoDL fallback via a new adapter for `codes.npy + *_item_ids.npy` and a bounded RQ-VAE feature-proxy runner. This can unblock Gate 0 plumbing, but it must not be overstated as a full visual CARD reproduction.
- AutoDL readiness was corrected to follow `autodl-cloud-deploy`: remote execution should use `/root/autodl-tmp/Sec_phrase`, record hardware specs and git commit, archive completed outputs to `/root/autodl-fs/audit_sid/<RUN_ID>/`, and let the remote runner script own automatic shutdown. The local state is `TRANSFER_READY / RUNNER_READY / FULL_REPRO_BLOCKED_NO_GPU`.
- AutoDL experiment queue is now batched beyond the first robust run: quick has 2 smoke runs, robust has 3 primary runs, sweep adds 5 seed/capacity sensitivity runs, and quality adds 2 extension runs. The refreshed bundle `audit_sid_autodl_20260518_192752.tar.gz` contains the matrix, runners, dataset shards, and CARD fallback, with readiness report checksums recorded.
- Local CARD CPU smoke exposed an upstream source-integrity blocker: the public CARD clone is missing `rqvae4/models/rq.py` and `rqvae4/models/vq.py`, so CARD RQ-VAE entries are now guarded by `check_card_source.py` and skipped by default on AutoDL rather than aborting the ReSID queue. Latest bundle is `audit_sid_autodl_20260518_193310.tar.gz`.
- AutoDL prelaunch code review found and fixed launch-safety blockers before any GPU spend: preflight now hard-fails without CUDA/dependencies, remote archive/shutdown failures are no longer swallowed, strict summary requires coverage plus D1-D5a outputs, and robust/sweep/quality hard-stop while CARD/Cluster-A source is incomplete. Current bundle is `audit_sid_autodl_20260518_195614.tar.gz`; only `QUEUE_MODE=quick` is recommended until CARD or another Cluster A path is repaired.
- Tokenizer dataset-category audit downgraded `Musical_Instruments` from paper-facing primary evidence to development/quick-smoke evidence. TIGER/GRID/GenRec/CapsID-style public SID work centers on Beauty/Sports/Toys, while ReSID's processed dataset also includes `Sports_and_Outdoors`, `Toys_and_Games`, and `Beauty_and_Personal_Care`. Before robust/sweep/quality AutoDL runs, add at least one canonical vertical, preferably ReSID Amazon-2023 `Sports_and_Outdoors`.
- Canonical vertical staging is now in place: ReSID processed `Sports_and_Outdoors` and `Beauty_and_Personal_Care` were downloaded, copied to `/Volumes/TU280Pro/Research/DataSet/ReSID-dataset`, symlinked back into `_gate0_repos/ReSID-dataset`, and schema-audited with zero missing item-feature joins. `Sports_and_Outdoors` is the next AutoDL canonical data-readiness run; `Beauty_and_Personal_Care` is optional after Sports is stable.
- AutoDL no-GPU staging completed on `ssh -p 10197 root@connect.westc.seetacloud.com`: the slim code bundle was transferred and verified, ReSID Musical/Sports/Beauty datasets were downloaded remotely via `hf-mirror.com`, normalized artifacts were generated, and preflight reports `ASSETS_READY RUNNER_READY`. Full GPU execution is explicitly blocked because `torch.cuda.is_available=False`.
- CARD's missing `rqvae4/models/rq.py` and `rqvae4/models/vq.py` can be resolved without GPU. A tracked repair utility now reconstructs the missing source files, patches PyTorch 2.6+ checkpoint loading, and passes local source/import/NURQVAE/tiny full-run CPU smoke plus remote no-GPU `CARD_SOURCE_READY` preflight on port 10197. This clears the file-integrity blocker but remains a compatibility fallback, not a faithful CARD reproduction claim.
- Prelaunch queue review found that CARD canonical entries were still defaulting to Musical metadata. The AutoDL CARD runner and matrix now pass `DATASET_NAME=Sports_and_Outdoors` for canonical/robust/sweep/quality so GPU runs align with the reviewer-facing canonical vertical decision.
- The first AutoDL robust launch on port 10197 did not complete Gate 0. Sports FAMAE 1 epoch finished and saved a checkpoint, but GAOQ is CPU-only and stalled with RTX5090 idle before writing `item_code_mapping.parquet`; `gate0_summary.csv` remained 1 byte. Remote API inspection shows `KMeansConstrained` defaults to `n_jobs=1`, so the tracked runner now patches ReSID at runtime to pass `GAOQ_KMEANS_N_JOBS` while preserving `use_balancedkmeans=true` as the formal default.
- Gate 0 is closed at artifact-feasibility level on `Sports_and_Outdoors`: `card_rqvae_feature_proxy`, `resid_gaoq_unbalanced_proxy`, and sanity controls all export SID assignments with full item coverage and D1-D5a metrics. The diagnostic suite already separates behaviors: ReSID proxy is collision-free with 151,411 unique SIDs, CARD compact has high collision/duplication pressure with 18,298 unique SIDs, and sanity baselines bracket semantic purity and collision extremes. This is not yet final paper evidence because both method runs are proxies, but it is enough to proceed to case-study writing and quality strengthening.
- Re-audit corrected the previous Gate 0 closure: under `docs/PROJECT_SPEC.md`, the Sports matrix is pipeline-unblocked evidence, not a Gate 0 pass. `card_rqvae_feature_proxy` is a controlled proxy/failure case rather than a real Cluster A method, and `resid_gaoq_unbalanced_proxy` is not faithful balanced ReSID/GAOQ. Gate 0 / 0A remain open until a real TIGER/RQ-VAE/GRID-style Cluster A export and a real named Cluster B export are joinable and diagnostic-ready. Further CARD compact proxy strengthening is paused because it would not fix the coverage gap.
- Gate 0 is now passed for artifact feasibility after replacing proxy evidence with real-method exports: GRID official-module RQ-KMeans on Amazon-2023 `All_Beauty` exports 5,000 joinable Cluster A SIDs with D1-D5a metrics, and ReSID balanced GAOQ on processed `Musical_Instruments` exports 23,742 joinable Cluster B SIDs with D1-D5a metrics. This does not close Gate 0A or paper readiness because the methods are cross-dataset, D3 remains category-purity proxy, GRID bypasses its Hydra/TFRecord stack, and stability/case-study strength still need adjudication.
- A real Cluster A candidate path is now locally smoke-passed: Amazon-2023 All_Beauty metadata/reviews are converted to text embeddings with a local MiniLM model, then the public GRID clone's official `MiniBatchKMeans`, `SquaredEuclideanDistance`, and `KMeansPlusPlusInitInitializer` classes produce residual k-means SIDs and D1-D5a metrics. The 32-item smoke is not a Gate 0 pass, but it replaces the previous empty Cluster A slot with a reviewable export path that can be scaled on AutoDL after code review and staging.
- AutoDL no-GPU staging for Gate 0A is transfer-verified: the remote now has the GRID clone, All_Beauty raw gzip files, the MiniLM model, GRID/Gate0A scripts, and AUDIT-SID source modules. The remote `preflight_gate0a_grid.sh` reports READY, so the next GPU window can launch a 20k/50k GRID real Cluster A strengthening run without additional file transfer.
- Local documentation was reorganized around current decision entrypoints: `docs/DOCUMENT_INDEX.md` is the live map, `docs/GATE0_DECISION.md` is the current Gate 0 verdict, and older `docs/GATE0_RESULTS*` files are retained as proxy-matrix history rather than active verdicts. `README.md` and `START_HERE_AUDIT_SID.md` now point first to the Gate 0/Gate 0A materials needed for the next AutoDL run and mark `Musical_Instruments` as quick-smoke rather than paper-facing primary evidence.
- Historical docs and ignored local artifacts are now indexed without physical moves: `docs/ARCHIVE_INDEX.md` groups timestamped planning, audit, proxy, AutoDL, fallback, and older idea-discovery files, while `docs/ARTIFACTS_INDEX.md` marks `_gate0_artifacts/` evidence, debug-provenance, and post-decision cleanup candidates. This keeps provenance stable while making the active Gate 0A path readable.
- AutoDL Gate 0A ReSID canonical work is active: Sports_and_Outdoors FAMAE has finished and balanced GAOQ is CPU-running with `GAOQ_KMEANS_N_JOBS=30` but no mapping yet; Beauty_and_Personal_Care FAMAE-only checkpoint prep was launched with `STOP_AFTER_FAMAE=1` to use idle GPU without starting a second CPU-heavy GAOQ.
- AutoDL Gate 0A GRID clean batch passed on the RTX 5090 instance: real GRID/RQ-KMeans Cluster A exports completed for All_Beauty 20k seeds 42/43/44 and 50k seed42, all with complete metadata/interactions joins and D1-D5a metrics. The 20k duplicate SID rate ranges from 0.1524 to 0.1748, and the 50k run has 37,146 unique SIDs with duplicate SID rate 0.2571. This materially strengthens Cluster A scale/stability evidence but does not close Gate 0A because dataset alignment, true D3, and ReSID canonical evidence remain open.
- While Sports_and_Outdoors balanced GAOQ remains the canonical ReSID Cluster B blocker, idle GPU time was converted into FAMAE checkpoint backlog rather than waiting: Sports seeds 43/44 and Beauty seeds 42/43/44 completed FAMAE-only with `STOP_AFTER_FAMAE=1`. These checkpoints are useful follow-up assets, but they are not a Gate 0A pass until a faithful GAOQ mapping is exported and diagnosed.
- ReSID Sports balanced GAOQ is CPU-bound but not currently using the 30-core AutoDL allocation well: after about 49 minutes it had no errors and no mapping, with only three active loky workers. A local opt-in runtime patch now prepares level-2 group parallelism across GAOQ's independent level-1 clusters while preserving faithful defaults; it should be remote-smoked before replacing the current canonical run.
- Gate 0A Cluster B route pivot is now active: ReSID `Sports_and_Outdoors` exact balanced GAOQ and the optimized v2 attempt were both preserved and stopped after showing the same CPU-bound `KMeansConstrained` bottleneck. ReSID remains valid real Cluster B smoke evidence on `Musical_Instruments`, but Sports exact balanced GAOQ is no longer treated as the blocking path; the next route is either strict smaller-dataset wording or a more controllable same-cluster tokenizer/codebook method.
- D3 is no longer only a category-purity proxy. `src/audit_sid/metrics.py` now builds item-item co-occurrence top-k collaborative neighbors and reports per-depth SID-prefix recall in `d3_alignment.csv`, while retaining category purity as auxiliary semantic metadata. Unit tests pass, and local ReSID Musical plus GRID All_Beauty 5k artifacts both emit D3v2 tables. This reduces the D3 Gate 0A blocker but does not solve same-dataset method alignment or the Cluster B Sports pivot.
- Gate 0A core is conditionally closed for a conservative resource-demo framing, not for a same-dataset leaderboard: GRID official-module RQ-KMeans covers Cluster A on All_Beauty, ReSID/GAOQ covers Cluster B on smaller Musical_Instruments, and sanity baselines plus D1-D5a/D3v2 support a ReSID-vs-sanity case study. CARD compact feature proxy remains a controlled stressor/backlog, not faithful named-method evidence.
- The ReSID-vs-sanity Musical case study is now paper-table ready at diagnostic level: D2/D5a catch the mod-collision hash collapse, D3v2 separates category/collaborative grouping from collision-free capacity, and D4 shows ReSID preserves unique SIDs across head/mid/tail. The safe claim is diagnostic tradeoff visibility, not downstream recommendation superiority.
- The first CIKM resource paper plan is drafted around the conservative Gate 0A route: title, abstract, claim-evidence matrix, 4-page structure, figure/table plan, and red-line wording are in `docs/CIKM_RESOURCE_PAPER_PLAN.md`. The plan explicitly frames the work as toolkit/resource evidence, not same-dataset leaderboard or faithful CARD/ReSID-Sports reproduction.
- A citation scaffold now exists with source links for TIGER, ReSID, GRID, CARD, and DIGER plus explicit verification rules. It deliberately avoids generating BibTeX from memory; all citation metadata remains to be checked before LaTeX writing.
- DACT local artifact smoke passed as optional D6 drift evidence, not as a replacement for the Cluster B main line. On bundled Tools snapshots, 0.6 -> 0.7 common-item SID churn is `2271/9610 = 0.236316`, while the 0.7 artifact has only `3` full-collision groups / `6` collision items. The reusable D6 tool is `tools/autodl_audit_sid/compute_sid_churn.py`.
- AutoDL no-GPU delta sync is verified on `ssh -p 10197 root@connect.westc.seetacloud.com`: docs/refine-logs/src/tools/tests and DACT smoke artifacts are synced to `/root/autodl-tmp/Sec_phrase`, remote unit tests pass, D6 churn recomputes, and no-GPU preflight reports `ASSETS_READY RUNNER_READY` with `torch.cuda.is_available=False`. Do not launch queues until `REQUIRE_CUDA=1` preflight passes.
- AutoDL GPU quick smoke completed after the RTX 5090 instance came online: `REQUIRE_CUDA=1` preflight passed, screen `audit_sid_quick_20260519_101555` ran without the auto-shutdown wrapper, and evidence was pulled locally. New quick rows show ReSID Musical balanced GAOQ with 23,742 unique SIDs, zero missing joins, and zero full collisions; CARD Musical compact proxy has 4,891 unique SIDs, 0.793994 duplicate SID rate, and 0.919299 full-collision rate. This remains smoke/provenance evidence only; robust/sweep/quality are still out of scope.
- Post-run subagent review found no blocker. It fixed one documentation consistency issue in the CIKM paper plan timestamped/latest pair, verified DACT/D6, D3/metrics, ARIS fixed/latest copies, `MANIFEST.md`, and public-stage data hygiene, and reproduced the DACT churn smoke. `docs/AUDIT_SID_EXTERNAL_AUDIT_HANDOFF.md` is now the external audit entry point.
- Third external audit identified cross-dataset evidence as the main structural risk. A local CPU same-dataset GRID Musical row now exists: official GRID MiniBatchKMeans over ReSID Musical processed feature-text embeddings exports 23,742 SIDs with zero missing joins, 3,749 unique full SIDs, 0.842094 duplicate SID rate, and 0.976876 full-collision rate. Compared on the same item universe, ReSID GAOQ remains collision-free with 23,742 unique SIDs. This materially improves diagnostic credibility, but it is a controlled feature-text row rather than faithful raw-text TIGER/GRID reproduction.
- After commit `303e1fc`, the no-GPU AutoDL instance was used only for staging: docs/refine-logs/src/tools/tests plus manifest/findings were synced to `/root/autodl-tmp/Sec_phrase`, structured paths were verified by remote unit tests, and `/root/miniconda3/bin/python` ran 6 tests OK. No GPU queue was launched; old dead screen sessions were left untouched.
- MovieLens portability smoke passed locally without GPU. On a bounded MovieLens-25M slice with first 1,000,000 ratings and 10,000 movie items, sanity SID baselines have complete metadata/interaction joins and D1-D5a/D3v2 all run unchanged. This supports only the resource/toolkit schema-portability claim beyond Amazon, not a main SID tokenizer benchmark.
- The paper-facing metric scope is now tightened: D1-D6 cover item-to-SID artifact audit dimensions, not full SID system quality. Main CIKM text should focus on D1-D5a; D6 is optional; D2 is collision profile rather than causal harm; D3v2 is a diagnostic proxy; D5b/D7 require generator outputs; online impact, full generator dynamics, unified search-rec, and industrial policy/fairness remain out of scope.
- CIKM Resource page-limit rules are now verified from the official track page: 4 pages includes appendices and acknowledgments, while references and GenAI Usage Disclosure are unlimited. AUDIT-SID should be written as a no-appendix 4-page paper with only Table 1 method coverage and Table 2 same-item Musical diagnostics in the PDF; full run tables now live under `paper_assets/tables/` for the artifact repository.
- AUDIT-SID now has a generated paper reference file at `paper_assets/references/audit_sid_references.bib`, derived from verified arXiv/official pages recorded in `docs/CITATION_AUDIT.md` and `docs/BIBTEX_AUDIT.md`. Final submission still needs a citation drift recheck, but BibTeX generation is no longer an open blocker.
- A first ACM CIKM Resource draft now exists under `paper/` and compiles locally to a 3-page PDF. The draft uses the no-appendix strategy, includes the two PDF tables required by `docs/CIKM_RESOURCE_FORMAT_AUDIT.md`, and absorbs the reference-refresh warning by citing identifier foundations, RecList-style diagnostics, collaborative tokenization, and industrial SID deployment.
- The paper figure/table plan is now fixed at one figure plus two tables: Fig. 1 should show the AUDIT-SID artifact contract and D1-D7 diagnostic map; Table 1 should be a facet-aware coverage matrix using the refreshed A/B1-B4/C/D taxonomy; Table 2 should remain the only diagnostic result table, using the same-item Musical GRID feature-text vs ReSID GAOQ contrast. DACT churn, MovieLens portability, GRID scale, and sanity-control tables should stay in the artifact repo rather than the 4-page PDF.
- The figure/table plan has now been applied in `paper/`: Fig. 1 shows the artifact pipeline and D1-D7 boundary, Table 1 is facet-aware across A/B1-B4/C/D/control roles, and Table 2 is a compact same-item Musical artifact-profile table. The draft still compiles to 3 pages; no additional experiment is needed for this integration, while D7 remains future work unless generator outputs are available.
- SID reference refresh found that the current citation scaffold is too narrow for a credible CIKM Resource paper. The main paper should cite at least one pre-TIGER identifier/indexing anchor, one diagnostic-tooling anchor such as RecList, representative 2024 collaborative-tokenizer work such as CoST or LETTER, and recent 2026 SID work on collision qualification, industrial use, drift/staleness, and input/output bottlenecks. The safe related-work story is field-wide artifact audit, not `TIGER -> ReSID -> AUDIT-SID`.
- The method taxonomy is now updated after the reference refresh. Keep Cluster A as canonical SID generation, split Cluster B into B1 collaborative/predictability, B2 collision/capacity, B3 ranking/differentiable/retrieval, and B4 bottleneck/interface facets, keep Cluster C for drift/staleness, and keep Cluster D for industrial/search deployment surfaces. R0 foundation/evaluation papers are not runnable SID methods. Current evidence covers D1-D5a plus optional D6; D7 generator/retrieval behavior remains an interface hook requiring `generator_outputs`.
- On 2026-05-20, the local prefix-matched full Musical GRID run under `_gate0_artifacts/grid_same_dataset_runs/matched_capacity_grid_32_1280_1280_seed42_20260520_000320/grid_export` was stopped by user request. Two CPU `run_grid_rqkmeans_direct_export.py` processes with `--per-level-widths 32,1280,1280` were terminated by SIGTERM, and no metrics/files were produced in the run directory. This stopped run must not enter evidence tables.
- A read-only subagent review found no blocker in the figure/table integration. Its minor findings were applied: Table 1 now aligns B3 with D3/D5a/D7, Table 2 labels ReSID as a bounded export, and the capacity sentence no longer implies a full head/mid/tail table in the PDF. The draft still compiles to 3 pages.
- The paper body and figure/table package have been upgraded after review feedback. Fig. 1 is now a generated vector PDF from `tools/paper_figures/generate_audit_sid_pipeline.py`, Table 1 is a paper-facing facet/evidence/boundary matrix, Table 2 includes item counts and diagnostic columns, and Table 3 summarizes the reviewer-facing artifact package. The compiled PDF is now 5 pages total, with the body filling through page 4 and references/GenAI on page 5. A strict claim audit in `docs/PAPER_STRICT_CLAIM_AUDIT.md` passes: current numbers match `paper_assets/tables/table2_musical_diagnostic.csv`, and the draft avoids leaderboard/reproduction/serving-latency/online-impact overclaims.
- Two internal CIKM 2026 Resource Track review rounds are complete before external simulated review. Round 1 scored 7.1/10 and exposed resource-packaging and table-naming issues. After fixes, Round 2 scores the draft at 8.0/10: Fig. 1 is now explicitly tied to comparable audit artifacts, Table 1 has standardized v0 status labels, Table 2 includes metric directionality plus D5a prefix counts, Table 3 is a reviewer action checklist, and GenAI disclosure no longer suggests unverified claims. External simulated review has started and should use CIKM 2026 Resource Track as the fixed target.
- External simulated review target is also 8.0/10 for CIKM 2026 Resource Track. The external stage should be treated as a high-bar review of artifact utility, claim discipline, reproducibility, coverage, and page budget, not as a weak-accept threshold.
- External simulated review Round 1 did not request new experiments for 8/10. Resource and SID reviewers scored the pre-packaging draft 7.2/7.4, then 7.7 after packaging fixes, and both indicated 8.0 is reachable after commit/tag/push plus clean-checkout verification. The new public artifact path is `ARTIFACT_QUICKSTART.md` -> `requirements.txt` -> unit tests -> Fig. 1 regeneration -> `tools/verify_paper_artifact.py`; `_gate0_artifacts/` is now explicitly only a local full-rebuild cache.
- External simulated review Round 2 closes the requested 8/10 gate. The pushed tag `audit-sid-cikm-resource-v0.1` resolves to `d24dfec`; a fresh tag clone passes 6 unit tests, regenerates Fig. 1, passes the public artifact verifier, and remains clean. External reviewer scores are now 8.0/10 and 8.1/10 with no P0 blockers.
- Strong-accept planning update: the current score ceiling is evidence breadth, not packaging. The optional 8.5-target package is a third true named B2/B3 tokenizer artifact, a stronger same-dataset A/B panel, explicit use of existing GRID 20k three-seed stability, and a redesigned Fig. 1 that shows artifact contract, D1-D5a/D6/D7 boundary, and evidence maturity.
- Method-selection matrix added: `docs/METHOD_DIAGNOSTIC_SELECTION_MATRIX.md` now maps candidate SID/tokenizer methods to D1-D7, expected finding material, priority, and next screen action. It makes B2 collision/capacity methods the first screen because they can most directly strengthen D1/D2/D4 findings.
- B2/B3 method screen completed on 2026-05-19: no low-risk third named tokenizer should enter main evidence from the current public/local state. QuaSID, AdaSID, and CapsID are useful collision/capacity/variable-length SID motivations but no runnable artifact was found in this screen; DIGER's public repo is reference/illustrative and lacks data/checkpoints; CARD's repaired runner remains proxy/control unless the original `nu-rq-vae` path is repaired and reviewed. The near-term lift therefore shifts to same-dataset stability and sharper findings, not proxy method expansion.
- GRID Musical same-dataset evidence now has three local CPU seeds for the feature-text row. Seeds 42/43/44 all have complete joins; duplicate SID rate ranges 0.8327--0.8421 and full-collision rate ranges 0.9751--0.9769. This supports the paper claim that the high collision pressure is stable for this controlled artifact row, while the caveat remains that it is not faithful raw-text TIGER/GRID reproduction.
- Experiment-plan closure audit closes the current CIKM 2026 Resource v0 evidence package. Gate 0, conditional Gate 0A, D1-D5a/D3v2 implementation, same-item Musical contrast, GRID Musical three-seed stability, third-method screen, Fig. 1 redesign, public verifier, unit tests, and paper compile are all complete for v0. Remaining items are citation drift, real single-blind metadata, final copy-editing, and claim audit after text changes; do not reopen local/GPU experiments without a new explicit evidence gap.
- Results-based paper polish sharpened the current draft around three artifact-level findings: stable collision pressure in the controlled GRID Musical row, separation between collision-free capacity and collaborative-prefix alignment, and separation between tail-capacity allocation and prefix/fan-out structure. This improves the paper narrative without changing the claim boundary: no downstream superiority, faithful TIGER/GRID/CARD reproduction, or real serving-latency claim was added.
- A local additional-experiment preflight gate now exists at `tools/autodl_audit_sid/preflight_metric_inputs.py`. It validates future `sid_assignments`, `item_metadata`, and `interactions` inputs before metric execution and can emit a bounded D1-D5a smoke summary. This should be used before any future third-method or GPU experiment so incomplete/proxy artifacts do not silently become paper evidence.
- Third-method evidence is now explicitly gated: self-implemented paper-inspired tokenizers are controlled stressors, not named-method evidence. The current official-release scout keeps QuaSID, AdaSID, CapsID, and DIGER out of v0 main evidence because no reproducible official SID artifact/export is available. CARD original `nu-rq-vae` passes source/import/export-contract preflight locally, including a tiny CPU synthetic export, but still needs a real CARD input/checkpoint/export plus AUDIT-SID joins before it can become a third named method.
- CARD original `nu-rq-vae` is now closed as failed for AUDIT-SID v0 main evidence. After fetching the official CARD repo, the official tree at `b8ce0976` still lacks the quantizer modules required by the released NU-RQ-VAE wrapper (`rqvae4/models/rq.py`, `rqvae4/models/vq.py`, `rqvae4/vq.py`). Local execution depends on compatibility repairs, so it cannot be attributed as faithful CARD evidence without author-provided source or artifacts.
- Author artifact-request drafts are ready but not sent. `docs/AUTHOR_ARTIFACT_EMAIL_DRAFTS.md` contains neutral drafts for DIGER, QuaSID, AdaSID, and CapsID, signed `Timber Ding` and avoiding AUDIT-SID/CIKM/submission language. Sending still requires confirmed recipient addresses plus an email connector or SMTP route.
- Controlled stressors/controllers are now explicitly separated from named tokenizer evidence. `docs/CONTROLLED_STRESSOR_SELECTION.md` selects the existing collision, category, and popularity controllers as D1-D5a calibration rows, and treats `qualified_collision_probe` as the only near-term optional local addition if D2b needs strengthening.
- Controller plan revised after user alignment: generic sanity controls are not enough for the resource-paper narrative. `docs/CONTROLLED_STRESSOR_SELECTION.md` now defines a method-inspired controller suite: first `qualified_collision_probe` for interaction-qualified collision risk, second `capacity_budget_sweep` for capacity/collision/head-tail pressure, and third `variable_depth_cost_probe` for D5a depth/cost boundaries. All remain outside named-method coverage.
- The method-inspired controller suite now has local results. `qualified_collision_probe` shows GRID feature-text collision pairs have 3.86x train co-occurrence lift over popularity-matched non-collision pairs, while the collision-heavy hash control has only 1.19x lift. `capacity_budget_sweep` shows width-24 head-reserved allocation preserves head uniqueness but leaves tail unique ratio at 0.028190, separating capacity pressure from head-tail allocation. `variable_depth_cost_probe` is clean artifact evidence for D5a active-prefix boundaries but remains paper-optional.
- Paper controller integration is complete for the current draft. `paper/sections/3_resource_demo.tex` now uses qualified collision, capacity-budget, and variable-depth controllers as method-inspired stressor evidence, while `paper/sections/4_availability_limits.tex` preserves the boundary that these are not named-method coverage, causal collision-harm validation, generator-output D7 evidence, or downstream ranking claims. `paper/main.pdf` compiles to 5 pages total with body through page 4.
- The paper now contains an explicit controlled-stressor table in the main text. The 2026-05-19 20:06 pass rewrites the controller material from experiment-note prose into a compact Table 3 plus CIKM/SIGIR-style narrative, while preserving the same objective numbers and conservative claim boundaries.
- Table-structure audit confirmed that the earlier non-result tables were too text-heavy. The 2026-05-19 20:22 pass redesigns Table 1 as a facet-by-diagnostic matrix, Table 3 as a stressor-by-signal table, and Table 4 as an artifact checklist, leaving Table 2 as the main numeric case-study table.
- Paper structure/reference update: the 2026-05-19 21:49 pass restructures the CIKM Resource draft into five sections, expands cited references from 15 to 27 entries, keeps claims at artifact/resource scope, and compiles `paper/main.pdf` to 5 pages total with body through page 4 and references/GenAI disclosure on page 5.
- Paper layout update: the 2026-05-19 21:56 pass removes the forced page break before References and moves GenAI Usage Disclosure before References, so references now follow naturally after the disclosure without changing paper claims.
- CIKM template correction: the 2026-05-19 22:23 pass changes `paper/main.tex` from `sigconf,review,anonymous` to the CIKM short-paper literal `sigconf,natbib=true,anonymous=true`, removing review-mode line numbers while preserving the paper claims and layout.
- Full experiment-plan audit: the 2026-05-19 23:08 audit confirms the active CIKM Resource v0 experiment package is closed under conservative resource-demo framing. Required evidence is complete, explicitly negative-closed, or future-scoped; remaining work is submission hygiene and final paper QA rather than new local/GPU experiments.
- Round 4 latest-doc sync: the 2026-05-20 00:49 pass rechecked the current paper source before editing and synchronized fixed-name planning/state docs to the latest shape. The paper now has Fig. 1, Table 1, Table 2, and controlled-stressor Table 3; the older artifact-checklist Table 4 is deleted and replaced by a Section 5 clean-checkout verifier paragraph. Gate 3 remains resource-scoped diagnostic separability, matched-capacity GRID is caveat/future work, and no experiment was launched.
- Local cleanup before GitHub push: timestamped Round 4 snapshots were physically moved to `docs/archive/2026-05-20-round4-cleanup/`, `paper/archive/2026-05-20-round4-cleanup/`, `refine-logs/archive/2026-05-20-round4-cleanup/`, and `tools/archive/2026-05-20-round4-cleanup/`. Fixed-name latest docs, current paper sources, verifier scripts, and evidence tables remain in their active locations.
- Resource framing decision: AUDIT-SID CIKM v0 is now explicitly a Type 4 diagnostic/interface resource, not a Type 1 coverage resource. GRID/ReSID are worked examples, controlled stressors should be reframed as controlled mechanism probes, and any self-implemented tokenizer artifact must be labeled as an AUDIT-SID reference implementation/probe rather than a named-method reproduction.
- Author-contact logistics update: Gmail is connected for `jdding.cs@gmail.com`. Drafts, not sent messages, were created for DIGER, QuaSID, AdaSID, and CapsID artifact-availability requests. Recipient addresses were confirmed from public paper/PDF/arXiv source materials; draft IDs are recorded in `docs/AUTHOR_ARTIFACT_EMAIL_DRAFTS.md`.
- Author-contact framing update: after the diagnostic/interface resource framing freeze, author code requests are not required for CIKM v0 and should not be treated as evidence blockers. They are optional future-adapter contacts. The AdaSID Gmail draft was deleted because AdaSID and QuaSID share the same author route; QuaSID is retained as the single collision-qualification contact because it is closer to the D2b narrative.
- Author-contact send update: DIGER, QuaSID, and CapsID artifact-availability emails were sent from `jdding.cs@gmail.com` on 2026-05-20 CST. This remains optional future-adapter outreach; CIKM v0 claims and evidence must not depend on replies. Sent message IDs are recorded in `docs/AUTHOR_ARTIFACT_EMAIL_DRAFTS.md`.
- Author-reply update: Junchen Fu replied that DIGER plans to release datasets and the RQ-VAE checkpoint before SIGIR conference on July 20th. A short thank-you reply was sent. Treat this as future-adapter timing information, not current v0 evidence.
- Diagnostic terminology update: the paper-facing diagnostics are now D1 utilization, D2 aliasing, D3 neighborhood alignment, D4 popularity allocation, D5 structural cost, D6 temporal churn, and D7 generation traces. For CIKM v0, D1-D5 remain the main mapping-level probes; D6 is appropriate as optional DACT refresh-pair evidence and should not be expanded unless temporal drift becomes a claim; D7 stays a future generator-output hook. The updated draft compiles to 5 pages total, passes the public artifact verifier, and passes 16 unit tests.
- Title/abstract update: the paper-facing name is now `SIDInspect`, with title `SIDInspect: A Mapping-First Diagnostic Resource for Semantic-ID Tokenizers`; AUDIT-SID remains the internal repo/provenance name. The abstract now opens from the missing artifact-inspection interface, keeps the 23,742-item Musical worked example plus 3,749/0.9769/0.9751--0.9769 numeric anchors, and preserves the non-tokenizer/non-leaderboard boundary.
- SIDInspect terminology/code sync: active paper prose, Fig. 1, reviewer quickstart, artifact manifest, and CLI descriptions now use SIDInspect plus D1-D5 diagnostic-probe terminology. Legacy `audit_sid`, `collision`, and `d5a_deployment_cost.csv` names are intentionally retained as provenance-compatible implementation labels. Verification passes: Fig. 1 regeneration, paper table rebuild, public verifier, 16 unit tests, `git diff --check`, and `paper/main.pdf` compile.
- SIDInspector rename update: the current paper-facing title/tool name is corrected to `SIDInspector: A Mapping-First Diagnostic Resource for Semantic-ID Tokenizers`. Active paper, Fig. 1 generator, README, quickstart, artifact manifest, verifier output, and CLI-facing descriptions now use SIDInspector while AUDIT-SID remains the repo/provenance name. This is a naming correction only; it does not change experiments or claims.
- External LM simulated review completed for CIKM 2026 Resource Track. DeepSeek V4 Pro Round 1 scored 7.5/10 and requested minor fixes around variable-length SID scope, D6/D7 status, and portability evidence. After local text/table fixes and verification, Round 2 scored 8/10 with Accept and confidence 4/5; no third round is needed under the stop rule.
- D1-D6 coverage audit finding: current SID method papers' main artifact-level problem claims map cleanly to SIDInspector's D1--D6 boundary, but only under an artifact-inspection interpretation. Utilization/collapse maps to D1, aliasing and qualified collision maps to D2 plus the mechanism probe, collaborative neighborhood mismatch maps to D3, head-tail capacity maps to D4, prefix/trie structure maps to D5, and refresh/staleness maps to D6. Generator-output behavior, measured serving latency, downstream ranking impact, and unified search-rec tradeoffs remain outside D1--D6 and should be treated as D7/system evaluation.
- Reference audit finding: the current CIKM draft now has 27 active cited references with no missing or uncited keys. The set adequately covers the last two years of SID/tokenizer work through CoST, LETTER, ETEGRec, GRID, DiscRec, joint search-rec SID, ReSID, DIGER, DACT, QuaSID, CARD, AdaSID, CapsID, AsymRec, ACERec, SID staleness, SID-Coord, and Snapchat SID. Five metadata drifts were fixed in both paper and artifact BibTeX files: How-to-Index authors, Better Generalization authors, CoST title/authors, LETTER title/authors, and Snapchat SID title/authors. Older RecList/Elliot/TIGER/identifier papers should remain because they are role anchors, not stale substitutes for recent tokenizer work.
- R3 review absorption finding: the newest simulated CIKM Resource review lowers the risk assessment to 5/10 because the resource still has thin named-method coverage, a capacity-mismatched GRID/ReSID worked example, and no downstream validation for D3. The actionable correction is not to over-expand claims; current paper edits make D3 a triage/diagnostic warning signal, surface the category-prefix D3 inversion, formalize the adapter schema, and keep D6/D7 outside main demonstrated contributions. A matched-capacity GRID ablation was attempted but stopped locally without output, so it cannot be used as evidence.
- Matched-capacity GRID finding: the AutoDL RTX 5090 run for the R3 W2 critique completed with per-level widths 32/1280/1280 on the same 23,742 Musical items. Capacity expansion materially reduces but does not remove aliasing: unique full SIDs rise to 9,874 and D2 full-code aliasing drops to 0.778452, while D3 L1 reaches 0.0796 and D4 tail unique-SID ratio reaches 0.639064. This supports the conservative interpretation that the original GRID row was capacity-sensitive, but the diagnostic profile is not explained away by capacity alone.
- SIDInspector naming correction finding: after integrating the matched-capacity row, the active PDF and verifier briefly regressed to the older `SIDInspect` label. Active paper/artifact files have been corrected back to `SIDInspector`, and the verifier now reports `SIDInspector/AUDIT-SID public artifact verification passed`.
- 17-day 8/10 lift-plan finding: after user calibration that the real deadline has 17 days and execution capacity is higher than the earlier conservative plan assumed, the active strategy is more ambitious. `SIDInspect` remains the paper-facing name, and the main lift package is now: elevate the category-prefix D3 inversion into the central diagnostic finding, run/record matched-capacity GRID as the first defense experiment when GPU is available, add vertical D3 replication and bounded D3-vs-ranking context if they clear gates, and treat reference implementation or third named-method breadth as stretch work. This reopens targeted evidence gaps, but new claims still require spec-to-experiment support before entering the paper.
- Matched-capacity GRID AutoDL finding: B2 completed on the user-provided port-21551 RTX 5090 instance. The `32,1280,1280` GRID/RQ-KMeans feature-text row has complete joins for 23,742 Musical items, 9,874 unique full SIDs, duplicate SID rate 0.584113, D2 full-code aliasing 0.778452, D3 L1 weighted co-occurrence prefix recall 0.079595, D4 tail unique-SID ratio 0.639064, and D5 prefixes `32;9300;9874`. Capacity explains part of the earlier GRID collision pressure, but it does not eliminate aliasing or close the D3 gap to ReSID/category-prefix rows; use this as a capacity-sensitive artifact-profile result, not a method-ranking claim.
- Post-matched-capacity claim audit finding: the active paper-to-evidence audit passes after adding `GRID ft-cap`. One display mismatch was fixed: D3 L1 should be `.0796` from weighted co-occurrence recall `0.079595`, not `.0795`. The abstract now states the capacity ablation result directly, which reduces R3 W2 risk without changing the core scope boundary.
- B4 vertical replication finding: the port-21551 AutoDL All_Beauty 20k run completed and was pulled back locally. On the GRID/RQ-KMeans All_Beauty seed42 export, D3 L1 weighted neighborhood alignment is 0.0811, while the deterministic category-prefix control reaches 0.9684, the mod-collision hash control reaches 0.0046, and the popularity-balanced control reaches 0.2500. This supports the D3 inversion as a portable diagnostic stressor signal, but the All_Beauty category metadata is coarse (`coarse_category_levels=true`, `level0_unique=2`), so the result must not be overstated as rich hierarchy or downstream validation evidence.
- B3 ranking-context finding: a bounded Musical prefix-neighborhood retrieval proxy now runs locally via `tools/autodl_audit_sid/run_d3_ranking_context.py`. On 1,000 users at depth 1, D3 is broadly aligned with candidate coverage: category-prefix has D3 0.4393 and candidate coverage 0.6047, ReSID has D3 0.1308 and coverage 0.3647, GRID ft-cap has D3 0.0761 and coverage 0.2590, and GRID feature-text has D3 0.0525 and coverage 0.1964. Hit@20 remains low for all rows (0.0471 category-prefix, 0.0421 ReSID, 0.0311 GRID ft-cap, 0.0235 GRID feature-text), so this is ranking-context evidence for D3 as a diagnostic artifact signal, not downstream Recall/NDCG validation.
- Sports proxy/control D3v2 supplement finding: the port-21551 AutoDL supplement completed and was pulled back locally under `_gate0_artifacts/vertical_replication/sports_proxy_d3v2_20260520/`. On 151,411 Sports items, category-prefix D3 L1 weighted is 0.4827 and popularity-balanced is 0.4811, while ReSID unbalanced proxy is 0.1075 and CARD feature proxy is 0.0514. The category control uses real hierarchy columns (`category_l1`, `category_l2`, `category_l3`), but both learned rows are proxy exports, so this is directional artifact-supplement evidence only, not named-method coverage or a main paper benchmark.
- Paper writing update: the active draft now folds B3/B4/Sports into the paper with conservative boundaries. The abstract and introduction elevate the category-prefix D3 inversion as the memorable diagnostic finding; Section 4 records All_Beauty and Sports as auxiliary portability/supporting evidence without adding a new main table; Section 5 states that the B3 prefix-neighborhood retrieval probe contextualizes D3 but is not Recall/NDCG validation. Verification after the writing pass: `paper/main.pdf` compiles to 5 pages, `tools/verify_paper_artifact.py` passes, 18 unit tests pass, and the log scan finds no undefined references/citations, overfull boxes, or stale `SIDInspect` spelling in active paper files.
- Reviewer-attack wording update: subagent review identified several phrases that could still invite a leaderboard or faithful-reproduction reading. The active draft now says `two controlled public SID export examples`, describes rows as artifact profiles rather than tokenizer-quality rankings, labels GRID/ReSID as controlled/bounded exports in the method-space table, weakens B3 to prefix-candidate context rather than downstream validation, and keeps Sports out of the main evidence because its learned rows are proxy exports. Final checks after this patch: active-paper scan has no stale `SIDInspect` spelling or flagged leaderboard/D3-overclaim phrases, `paper/main.pdf` metadata title is `SIDInspector: A Mapping-First Diagnostic Resource for Semantic-ID Tokenizers`, and 18 unit tests pass.
- B3 robustness update: the Musical prefix-neighborhood retrieval context probe was rerun with 5,000 users and 500,000 co-occurrence pair events. The direction is stable: category-prefix has D3/candidate coverage `0.4383/0.6126`, ReSID `0.1353/0.3601`, GRID ft-cap `0.0760/0.2487`, GRID feature-text `0.0523/0.1859`, and hash `0.0039/0.0195`. Hit@20 remains low for all rows (`0.0496` category-prefix, `0.0444` ReSID, `0.0266` GRID ft-cap), so this strengthens D3 as ranking-context evidence but still does not validate D3 as Recall@K/NDCG.
- MovieLens D3 sanity portability finding: the existing local artifact `_gate0_artifacts/movielens_portability_smoke/ml25m_1mratings_10kitems/` is sufficient for a non-Amazon D3 sanity summary, so no rerun or raw-data copy was needed. Programmatic validation loaded all D1-D5/D3 CSVs and parquet inputs with zero coverage gaps across category-prefix, popularity-balanced, and mod-collision hash controls. Key D3 L1 weighted values are category-prefix `0.2788`, popularity-balanced `0.7698`, and hash `0.0038`; use this only as schema/probe portability evidence, not as named SID tokenizer, learned-tokenizer, or downstream Recall/NDCG evidence.
- All_Beauty 3-seed vertical D3 update: seed43/44 panels completed on the no-GPU AutoDL instance and were pulled back locally. Across GRID seeds 42/43/44 on All_Beauty 20k, GRID D3 L1 weighted is `0.0811/0.0872/0.0898` (mean `0.0861`), while category-prefix remains `0.9684`. This strengthens the second-dataset direction for the D3 inversion, but the category metadata is still coarse (`category` fallback, two level-0 values), so the safe claim remains diagnostic portability/stressor evidence rather than rich hierarchy validation or learned-tokenizer superiority.
- B5 release recheck finding: no new official third named-method artifact is ready for v0. DIGER still has only the main branch in the local `git ls-remote` check and no ready item-to-SID mapping/checkpoint package; the prior author reply remains a future-release signal near SIGIR/July 20. QuaSID, AdaSID, CapsID, and CARD still lack an official public mapping/checkpoint/export path that passes the third-method gate. Do not start B5 reference implementation for the CIKM sprint; keep B5 as release-watch/backlog only.
- Aggressive 8/10 acceptance-plan update: the project should not stop at the completed defensive probes. The upgraded plan keeps the conservative claim boundary but adds three harder closure items: B6 small ranking validation to test whether D3 has Recall/NDCG or candidate-generation grounding under a fixed mapping-dependent protocol; B7 third vertical with a real learned/export row if a joinable dataset is available; and B8 elevation of qualified aliasing as a backup memorable finding. B5 remains no-go as named-method evidence, but a clearly labeled SIDInspector reference RQ/RQ-VAE adapter is allowed if it is cheap and cannot be confused with a named-method reproduction.
- B6 fixed-reranker validation finding: the Musical 5,000-user run uses SID prefixes only as candidate generators and applies the same train-only co-occurrence/popularity reranker to every row. At depth 1 with the co-occurrence reranker, D3 has Spearman `0.9429` with candidate recall, `0.8857` with Recall@20, and `0.9429` with NDCG@20/MRR@20. This supports D3 as an early candidate-generation/ranking-context signal, not as trained generator Recall/NDCG validation.
- B7 third-vertical finding: Sports_and_Outdoors now has a real GRID/RQ-KMeans feature-text export at 20,000 items, not a proxy row. The run has zero metadata/interaction SID gaps, 8,165 unique full SIDs, duplicate SID rate `0.59175`, D3 L1 weighted `0.054982`, D4 tail unique-SID ratio `0.652840`, and D5 prefixes `128;7986;8165`. This strengthens cross-vertical GRID portability evidence but does not add a third named tokenizer.
- B8 qualified-aliasing review finding: the existing qualified collision probe can serve as a backup memorable finding only under interaction-qualified early-warning wording. The safe claim is that raw aliasing volume and behaviorally qualified aliasing are separable artifact properties: GRID feature-text collision pairs show `3.86x` train co-occurrence lift over popularity-matched non-collision pairs, while the hash control shows `1.19x`. Do not write this as causal downstream collision harm or QuaSID reproduction.
- B6/B7 code-review closure finding: independent code review found no critical or high computation error in the fixed-reranker D3 validation. The required fix was release/claim hardening: B6/B7 now have frozen `paper_assets/tables/table11_d3_ranking_validation.csv` and `paper_assets/tables/table12_sports_grid_vertical.csv`, the public verifier requires the B6/B7 scripts/docs/tests and checks the six-row B6 Spearman values plus Sports 20k zero-join-gap/D3/duplicate-SID row, and B6 wording is scoped to fixed-reranker Recall/NDCG rather than trained-generator validation. An explicit no-history target denominator test was added.
- Clarity-first writing update: after user instruction not to optimize prematurely for the 4-page limit, the active draft now treats page compression as a later pass. Abstract, Introduction, and Section 4 were rewritten so B4 All_Beauty, B6 fixed-reranker, and B7 Sports GRID answer reviewer questions around D3 robustness, ranking context, and vertical portability rather than appearing as an experiment log. A fresh zero-context claim audit found no arithmetic mismatches; its evidence-attachment warnings were addressed by freezing All_Beauty D3 in `table13`, adding protocol/config columns to `table11` and `table2`, updating Table 1 GRID coverage to include Sports, and extending the verifier. The PDF currently compiles to 6 pages by design.
- Second clarity-first writing polish: the active SIDInspector draft now has a cleaner abstract-to-Section-4 argument: exported SID mappings are reusable generator address spaces without a common inspection interface; SIDInspector supplies the mapping-first contract and diagnostic probes; the D3 category-prefix inversion is the main finding; B4/B6/B7 are supporting checks for portability, ranking context, and third-vertical GRID feasibility. Verification after the polish passes with a 6-page PDF, correct SIDInspector metadata, public artifact verifier, 24 unit tests, `git diff --check`, and no LaTeX undefined-reference/citation or overfull-box findings. No GPU run was launched.
- Sixth-round closure finding: W2/W3/W4/Lift5 are now addressed under strict boundaries. All_Beauty's 0.968 category-prefix D3 is written as a dataset-level alignment signal, not a tokenizer-quality claim. B6 has a bounded All_Beauty temporal-LOO replication with D3 Spearman `1.0000` against candidate recall and `0.8000` against fixed-reranker Recall@20/NDCG@20/MRR@20 across four rows. Figure 1 now previews the actual finding chain. `rqvae_minimal_reference` passes a full-Musical CPU gate with 23,742 items, 17,247 unique full SIDs, D2 aliasing `0.4401`, D3 `0.0650`, D4 tail `0.8831`, and D5 prefixes `32;2368;17247`; it is reference-adapter evidence only, not third named-method coverage. Public verification now covers table14/table15 and the RQ-min runner/test/docs.
- Seventh-round review absorption finding: the remaining high-ROI review points are now in the paper without adding experiments. Section 4 states the key dissociation that D2 aliasing improves across GRID ft -> GRID ft-cap -> RQ-min while D3 stays low, the All_Beauty B6 paragraph is cooled to supplementary directional evidence, Figure 1 labels are cleaned up, and the abstract ending emphasizes inspectable resource artifacts. Verification passes with a 6-page clarity draft, correct SIDInspector PDF metadata, public artifact verifier, 26 unit tests, `git diff --check`, and no LaTeX undefined-reference/citation or overfull-box findings. The next blocker is page compression rather than data.
- Pre-compression table-design finding: the paper tables were too close to execution notes, so the active draft now treats them as reader-facing claim surfaces. Table 1 is an evidence-role map; Table 2 is an artifact-profile table with explicit `Unique full SIDs` and D2--D5 columns; Table 3 is a short mechanism-check summary. Seed information is not discussed in body prose and appears only through `mean +- sd` where a repeated-run aggregate is shown. No experiment values changed.
- Figure/table naming cleanup finding: the active draft now avoids internal labels on the main visual/table surfaces. Figure 1 is a wider double-column landscape figure titled `From SID exports to diagnostics`; Table 1 is `SIDInspector artifact contents`; Table 2 is explicitly on `Amazon Reviews 2023 Musical Instruments` with `Unique SIDs`; and the mechanism-check material is prose rather than a separate Table 3. No evidence values changed.
- Single-column artifact-table alignment finding: no experiment or evidence value changed. Figure 1 is back to a compact single-column preview so the later page-compression pass does not pay for a two-column float. Table 1 is now a two-column resource list with `Resource part / Artifacts`, and Table 2 begins with `Artifact`, making the Table 1 artifact inventory map directly onto the Amazon Reviews 2023 Musical Instruments diagnostic rows. The former Table 2 D4/D5 crowding was fixed by widening the D4 column. Verification passes with a 5-page PDF, public artifact verifier, `git diff --check`, and a clean LaTeX log scan for undefined citations/references and overfull/underfull boxes.
- Body-polish and reference-format finding: no experiment value or claim boundary changed. The active draft received a sentence-level polish pass across the abstract, introduction, scope, demonstration, and limits sections while preserving the 5-page total PDF / 4-page body layout. A read-only subagent diagnosed the reference problem as duplicate arXiv identity fields in BibTeX (`journal`, `eprint`, arXiv DOI, and `note`). The fixed `paper/references.bib` and synced `paper_assets/references/audit_sid_references.bib` now keep pure arXiv entries as `@misc` with one `eprint` identity, remove arXiv DOI fields, and remove arXiv IDs from formally published entries and notes. Verification passes with a 5-page PDF, public artifact verifier, 26 unit tests, `git diff --check`, no duplicate arXiv/DOI patterns in `main.bbl`, and no LaTeX undefined-reference/citation or overfull/underfull findings.
